# taro-plugin-compiler-whitelist

> 

## 使用

### 安装
```
npm i taro-plugin-compiler-whitelist -D
```

### 使用插件
`/config/index.js`

```js
const config = {
  plugins: [
    ["taro-plugin-compiler-whitelist"]
  ]
}
```
